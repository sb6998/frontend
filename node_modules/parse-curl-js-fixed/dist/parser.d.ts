/// <reference types="node" />
import { ParsedCURL, RequestBody } from './interface';
import * as qs from 'querystring';
export default class CURLParser {
    private cURLStr;
    private yargObj;
    constructor(cURLStr: string);
    /** 预处理 */
    private pretreatment;
    /** 如果有误写的两个相同的，取最后一个 */
    private getFirstItem;
    getUrl(): string;
    getQuery(uri: string): qs.ParsedUrlQuery;
    getHeaders(): ParsedCURL['headers'];
    getMethods(): string;
    getBody(headers: ParsedCURL['headers']): RequestBody;
    parse(): ParsedCURL;
}
